const modulessrcfiles = ["./ezfetch.min.js", "./isthemoduleimporterworking?.js"]

for (let i = 0; i < modulessrcfiles.length; i++) {
    const currentsrcfile = modulessrcfiles[i]
    const script = document.createElement("script")
    script.src = script
    document.head.appendChild(script)
  }